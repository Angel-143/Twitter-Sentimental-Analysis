# Twitter-Sentimental-Analysis
Twitter Sentiment Analysis uses machine learning to classify tweets as positive or negative. The system cleans text, converts it using TF-IDF, and predicts sentiment through a trained model. A Flask backend and simple UI allow users to analyze any tweet instantly.
